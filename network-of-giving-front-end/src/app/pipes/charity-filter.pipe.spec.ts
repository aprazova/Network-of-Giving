import { CharityFilterPipe } from './charity-filter.pipe';

describe('CharityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CharityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
