import { LinesRangePipe } from './lines-range.pipe';

describe('LinesRangePipe', () => {
  it('create an instance', () => {
    const pipe = new LinesRangePipe();
    expect(pipe).toBeTruthy();
  });
});
