import { ImageNameDirective } from './image-name.directive';

describe('ImageNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageNameDirective();
    expect(directive).toBeTruthy();
  });
});
